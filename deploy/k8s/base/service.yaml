apiVersion: v1
kind: Service
metadata:
  name: workforce-mlops-api
  labels:
    app.kubernetes.io/name: workforce-mlops-api
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: workforce-mlops-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
